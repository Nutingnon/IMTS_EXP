# APN Project Progress (2026-03-11)

## Objective
Run through the APN training + inference pipeline using `scripts/APN/P12.sh` as launcher context, while preserving existing environment package versions whenever possible.

## What Has Been Completed

### 1. Entrypoint Validation
- Confirmed `scripts/APN/P12.sh` is a runnable launcher script.
- Confirmed the actual training/testing entrypoint is `main.py`.
- Confirmed `--is_training 1` path in `main.py` executes both training and testing.

### 2. Environment Strategy (Version Preservation)
- Followed the requested strategy: keep existing package versions unless missing.
- Current key environment snapshot at run time:
  - Python: `3.12.9`
  - Torch: `2.9.1`
  - CUDA: available (`torch.cuda.is_available() = True`)
- Missing packages that were added only:
  - `ptflops==0.7.4`
  - `wandb==0.21.1`
- Existing package versions were not downgraded/upgraded to match `requirements.txt` exactly.

### 3. Data Readiness Check
- Verified P12 data cache exists under `~/.tsdm`:
  - `~/.tsdm/rawdata/Physionet2012/set-{a,b,c}.tar.gz`
  - `~/.tsdm/datasets/Physionet2012/Physionet2012-set-{A,B,C}-sparse.tar`

### 4. Smoke Run (Direct `main.py`)
- Executed a minimal end-to-end smoke run (`itr=1`, `train_epochs=1`) successfully.
- Log file:
  - `logs/APN_P12_smoke_20260311.log`
- Result artifacts:
  - `storage/results/P12/APN/APN_P12_smoke_20260311/36_3/2026_0311_1443/iter0`
  - Includes `pytorch_model.bin`, `configs.yaml`, and eval metric json.

### 5. `P12.sh` Script Upgrade
Updated file:
- `scripts/APN/P12.sh`

Changes made:
- Added automatic available-GPU detection (with `nvidia-smi` fallback logic).
- Added optional manual override support:
  - `GPU_IDS="0 1" ./scripts/APN/P12.sh`
- Added run profiles:
  - `RUN_PROFILE=medium` (default):
    - `train_epochs=20`
    - `patience=5`
    - `itr=1`
  - `RUN_PROFILE=full` (original behavior):
    - `train_epochs=200`
    - `patience=10`
    - `itr=5`
- Preserved original full config as comments in script.
- Fixed dispatcher to use detected GPU array consistently.

### 6. Stability Verification (`RUN_PROFILE=medium`)
- Executed:
  - `RUN_PROFILE=medium ./scripts/APN/P12.sh`
- Status:
  - Completed successfully (exit code `0`).
- Runtime behavior:
  - Training ran and early-stopped at epoch 16/20.
  - Testing completed and checkpoint loading succeeded.
- Final reported metrics from this run:
  - `MAE: 0.37021493911743164`
  - `MSE: 0.3146660625934601`
- Result artifacts:
  - `storage/results/P12/APN/APN_P12_sl36_pl3_dm24_lr0.03_bs32_dp0.1_p20_tedim8/36_3/2026_0311_1551/iter0`

## How To Run Next

### Medium profile (default)
```bash
cd /home/yixin/study/imts/APN
./scripts/APN/P12.sh
```

### Full profile (original settings)
```bash
cd /home/yixin/study/imts/APN
RUN_PROFILE=full ./scripts/APN/P12.sh
```

### Manual GPU override
```bash
cd /home/yixin/study/imts/APN
GPU_IDS="0" RUN_PROFILE=full ./scripts/APN/P12.sh
```

## Notes
- Non-divisible last-batch warnings are expected and non-fatal.
- This progress file reflects completed work up to this point only.
